你现在帮我修改 Android 前端 UI 和页面流程。注意：我要做的不是“弹窗”，而是一个完整的电商导购 App。整体风格参考“天猫 AI 导购”的视觉设计，但页面形态应该是 App 主界面，而不是悬浮弹窗。

请先阅读整个项目结构，尤其是：

* Android 前端：

  * `app/src/main/java/.../ui/screens/chat/ChatScreen.kt`
  * `ChatInputBar.kt`
  * `ProductCard.kt`
  * `CartBar.kt`
  * 现有 ViewModel / Repository / Retrofit / ApiService / 数据模型
  * 导航相关代码
* 后端：

  * `backend/app/main.py`
  * `backend/app/api/`
  * `backend/app/routes/`
  * `backend/app/schemas/`
  * `backend/app/models/`
  * `backend/app/services/`
  * 所有和 chat stream、商品详情、购物车、加购、删除、结算相关的接口文件

重点要求：前端需要调用哪些接口、请求体是什么、返回字段是什么，必须从后端代码中读取和确认。不要凭空猜接口，不要硬编码假字段。如果当前前端模型和后端返回字段不一致，请修改前端数据类、ApiService、Repository 和 UI 适配逻辑。

## 一、整体目标

将当前 Android 前端改造成一个完整的电商 AI 导购 App，包含至少三个核心页面：

1. AI 导购聊天页
2. 商品详情页
3. 购物车页 / 加购确认页

整体风格参考天猫 AI 导购：

* 白色背景；
* 轻量电商风格；
* 大圆角卡片；
* 清晰的商品推荐卡；
* 流式思考过程；
* 底部固定输入栏；
* 用户可以从推荐结果跳转商品详情；
* 用户可以在商品详情页或商品卡片中加入购物车；
* 用户可以进入购物车查看、修改、删除商品，并进行结算相关操作。

## 二、AI 导购聊天页设计

聊天页是 App 的主页面，不是弹窗。

页面结构：

1. 顶部 App 标题栏：

   * 左侧显示 AI 助手图标和名称，例如“天猫AI”或当前项目助手名称；
   * 右侧可以有购物车入口、历史记录入口；
   * 不要设计成可关闭弹窗。

2. 用户消息：

   * 右侧浅灰圆角气泡；
   * 文本加粗或正常显示均可；
   * 适配长文本自动换行。

3. AI 思考过程：

   * 用户发送消息后，立即显示“分析用户需求”；
   * 流式返回过程中显示“正在分析用户需求”；
   * 展示分析内容，使用浅灰正文和左侧竖线；
   * 思考结束后显示“已完成思考”，右侧带下拉箭头；
   * 支持折叠/展开思考过程。

4. AI 最终回答：

   * 不是普通聊天气泡，而是浅色大卡片；
   * 标题加粗；
   * 内容分段展示；
   * 重点商品名、推荐理由、价格、卖点加粗；
   * 长内容可以滚动。

5. 商品推荐卡：

   * 如果后端返回商品列表或商品引用，前端要渲染成电商商品卡；
   * 每张商品卡包含：

     * 商品图片；
     * 商品标题；
     * 价格；
     * 标签/卖点；
     * 简短推荐理由；
     * “查看详情”按钮；
     * “加入购物车”按钮；
   * 点击商品卡或“查看详情”跳转到商品详情页；
   * 点击“加入购物车”调用后端购物车接口，不要只做前端假状态。

6. 底部输入栏：

   * 固定在底部；
   * 浅灰圆角输入框；
   * placeholder 为“输入或按住说话”；
   * 右侧根据状态显示发送按钮 / 停止按钮 / 语音按钮；
   * 流式生成时显示停止按钮；
   * 注意 `imePadding()` 和 `navigationBarsPadding()`，避免键盘遮挡。

## 三、商品详情页设计

需要新增或完善商品详情页。

触发方式：

* 用户在聊天推荐结果中点击商品卡；
* 用户点击“查看详情”；
* 用户在购物车中点击商品。

商品详情页必须从后端接口获取真实商品详情数据。请先在后端查找商品详情接口，例如可能是：

* `/api/products/{sku_id}`
* `/api/product/detail`
* `/api/goods/{id}`
* 或其他现有接口

以实际后端代码为准。

详情页 UI 要包含：

1. 顶部导航栏：

   * 返回按钮；
   * 标题“商品详情”；
   * 购物车入口。

2. 商品图片区：

   * 大图展示；
   * 如果图片为空，使用占位图。

3. 商品核心信息：

   * 商品标题；
   * 价格；
   * 品牌；
   * 类目；
   * 标签；
   * 库存状态；
   * 评分或热度，如果后端有字段则展示，没有则不硬编码。

4. 商品参数区：

   * 以列表或卡片形式展示规格参数；
   * 例如颜色、版本、容量、屏幕、电池、快充、影像参数等；
   * 字段名称和结构以后端返回为准。

5. 推荐理由 / AI 解读：

   * 如果后端返回 recommendation_reason、selling_points、summary 等字段，则展示；
   * 没有字段则不要编造。

6. 底部操作栏：

   * “加入购物车”按钮；
   * “立即购买”按钮，如果后端没有购买接口，可以先做 UI 占位并标注 TODO；
   * 加购成功后显示 Toast/Snackbar，并可跳转购物车。

## 四、购物车页设计

需要新增或完善购物车页。

入口：

* 聊天页顶部购物车图标；
* 商品详情页顶部购物车图标；
* 商品卡加购成功后的“去购物车”。

购物车接口必须从后端代码读取。请查找后端是否已有：

* 查看购物车；
* 添加商品；
* 删除商品；
* 修改数量；
* 清空购物车；
* 结算/生成订单。

以实际后端接口为准修改前端 ApiService。

购物车页 UI 包含：

1. 顶部栏：

   * 返回按钮；
   * 标题“购物车”。

2. 商品列表：

   * 商品图片；
   * 商品名称；
   * 规格信息；
   * 单价；
   * 数量；
   * 小计；
   * 删除按钮；
   * 数量加减按钮。

3. 底部结算栏：

   * 总价；
   * 商品数量；
   * “去结算”按钮。

4. 空购物车状态：

   * 展示空状态插画或文字；
   * 提供“去逛逛 / 返回导购”按钮。

5. 交互要求：

   * 加购调用真实后端接口；
   * 修改数量调用真实后端接口；
   * 删除商品调用真实后端接口；
   * 结算调用真实后端接口，如果后端已有；
   * 所有失败情况显示错误提示，不要静默失败。

## 五、接口适配要求

这部分非常重要：请不要凭空写接口。

请先阅读后端代码，整理出实际可用接口，包括：

1. 聊天流式接口：

   * URL；
   * Method；
   * Request body；
   * SSE event 格式；
   * 返回字段。

2. 商品详情接口：

   * URL；
   * Method；
   * Path/query/body 参数；
   * 返回字段。

3. 商品推荐/搜索接口：

   * URL；
   * Method；
   * 请求字段；
   * 返回商品列表结构。

4. 购物车接口：

   * 加购；
   * 查看购物车；
   * 修改数量；
   * 删除商品；
   * 清空购物车；
   * 结算。

然后根据后端真实字段修改 Android 前端：

* ApiService；
* DTO / data class；
* Repository；
* ViewModel；
* UI state；
* 页面导航参数。

如果后端接口字段命名和前端当前字段不同，前端要做兼容适配，例如使用 `@SerializedName` 或 kotlinx serialization 的对应注解。

## 六、导航结构要求

请为 App 设计清晰导航结构，例如：

* `ChatRoute`
* `ProductDetailRoute(skuId)`
* `CartRoute`

从聊天页商品卡跳转：

* `ChatScreen -> ProductDetailScreen`

从商品详情页：

* 返回聊天页；
* 加入购物车；
* 跳转购物车页。

从购物车页：

* 返回上一页；
* 点击商品进入商品详情页；
* 修改数量、删除商品、结算。

请优先复用现有 Navigation 结构。如果当前没有完整 Navigation，请在不破坏项目结构的前提下补充。

## 七、流式 UI 状态模型

请检查当前 SSE 事件格式，设计前端状态模型，例如：

* `UserMessage`
* `AssistantThinkingStart`
* `AssistantThinkingStreaming`
* `AssistantThinkingDone`
* `AssistantAnswerStreaming`
* `AssistantAnswerDone`
* `ProductRecommendations`
* `ErrorMessage`

如果后端没有明确区分 thinking 和 answer，请根据现有事件字段做最小适配；不要为了 UI 大改后端。

## 八、Compose 编译和代码质量要求

1. 使用 Jetpack Compose。
2. 不要大规模重写无关代码。
3. 不要破坏现有后端通信逻辑。
4. 不要硬编码商品数据。
5. 图片为空时使用占位图。
6. 网络请求失败要显示错误状态。
7. 修复当前 Compose 编译错误，尤其注意：

   * `Modifier.weight(1f)` 只能用于 `Row` / `Column` 的直接子组件；
   * 不要访问 Compose internal 的 `weight` 属性；
   * 组件内部如果需要占满空间，优先通过外部传入 `modifier`。
8. UI 尺寸、颜色、圆角、间距尽量抽成统一常量。
9. 保证竖屏手机显示正常。
10. 长回复、长商品列表、购物车列表都要可滚动。
11. 底部输入栏和底部操作栏不要被系统导航栏或键盘遮挡。

## 九、验收标准

完成后请确保：

1. Android 项目可以成功编译；
2. App 能正常打开聊天页；
3. 聊天页可以发送消息，并展示后端 SSE 流式回复；
4. 思考过程能显示“分析用户需求 / 正在分析用户需求 / 已完成思考”；
5. 最终导购回答显示为卡片式内容；
6. 商品推荐显示为电商商品卡片；
7. 点击商品卡可以进入商品详情页；
8. 商品详情页展示后端真实商品字段；
9. 商品详情页可以调用真实接口加入购物车；
10. 聊天页商品卡也可以调用真实接口加入购物车；
11. 购物车页可以查看真实购物车数据；
12. 购物车页支持数量修改、删除商品和结算；
13. 所有接口字段以后端代码为准；
14. 不要出现硬编码假商品；
15. 运行 `./gradlew assembleDebug` 或 Android Studio Build 无 Kotlin/Compose 编译错误。

请先输出你从后端读取到的接口清单，包括 URL、Method、请求体、响应字段和对应前端文件修改计划。确认接口后，再开始修改 Android 前端代码。
